var deviceManager = require("app/entities/deviceManager")
//var utils = require("app/entities/utils");

/*var query = {};
if(request.body != null){
	query = request.body;
} else if(request.parameters.body) {
	query = request.parameters.body;
} else {
  query = request.parameters;
}
//return query;
//var documents = [];
var devices = deviceManager.getDevicesIdBySearch(query.inputTxt);
*/
var devices = deviceManager.getDevicesIdList();
return devices;

